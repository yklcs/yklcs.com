<aside class="sidenote"><slot /></aside>

<style lang="scss">
  .sidenote {
    grid-column: r-side-wide;
    margin: 0 0 0 1rem;
    border-left: 1px solid;
    border-color: var(--neutral-10);
    padding: 0 0 0 0.5rem;
    font-size: 0.875em;
    hyphens: auto;
  }
</style>
